function add(a, b) {
    const total = a + b;
    if (b > 5) {
        const sum = 26 + a + b;
    }
    else {
        const sum = 5 + a + b;
        var current = sum;
    }
    console.log(current);
    return total;
}
add(2, 5);